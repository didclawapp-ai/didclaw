# 复制为 .env.production 后执行 pnpm build，可把网关参数打进前端静态包（会出现在 JS 内，勿用于泄露场景）。
# Electron 便携版/安装版更推荐在应用内「网关本地设置」写入 gateway-local.json，无需重建 exe。

VITE_GATEWAY_URL=ws://127.0.0.1:18789
# VITE_GATEWAY_TOKEN=
# VITE_GATEWAY_PASSWORD=
# 逗号分隔，用于会话栏「模型」下拉；需与网关 chat.send 支持的 model 值一致
# VITE_CHAT_MODEL_OPTIONS=anthropic/claude-sonnet-4-5,openai/gpt-4.1
