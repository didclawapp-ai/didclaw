---
description: DidClaw 仓库总则 — 整合期协作、目录、品牌与 Git 纪律
alwaysApply: true
---

# DidClaw 项目总则（整合期）

## 仓库与目录

- 根仓库 **LCLAW**：方案与笔记在 `docs/`；客户端代码在 **`didclaw-ui/`**（已从 `lclaw-ui/` 重命名）。
- **`openclaw-src/`** 默认不纳入 Git（见根 `.gitignore`）；勿把大型上游拷贝误提交。

## 品牌与命名

- 对外产品名：**DidClaw**（不要写作「LCLAW UI」等带 UI 后缀的品名）。
- npm 包名：`didclaw`；环境变量前缀：**`DIDCLAW_*`**（与现有代码一致）。
- 权限与 Tauri 标识：`identifier`、权限文件名等与 **didclaw** 命名空间保持一致。

## 版本号与发版

- 遵循 **[语义化版本 2.0.0](https://semver.org/lang/zh-CN/)**：`主版本.次版本.修订号`（MAJOR.MINOR.PATCH）。
- **主版本（MAJOR）**：不兼容的协议/API/持久化格式变更，或需要用户明确迁移的破坏性改动。
- **次版本（MINOR）**：向后兼容的新功能、新配置项（旧客户端/旧用法仍可用）。
- **修订号（PATCH）**：向后兼容的缺陷修复与不影响契约的小改进。
- **0.y.z 阶段**：整合与公测期；在 **1.0.0** 之前仍可能有较大调整，应在 `CHANGELOG.md` 写清影响；同类破坏性变更优先用抬升 **MINOR** 表达（0.x 下 MAJOR 常为未来保留）。
- **单一事实来源（须三方一致）**：任何**正式发布或刻意改版本号**的提交中，须在同一提交内同步修改：
  - `lclaw-ui/package.json` → `version`
  - `lclaw-ui/src-tauri/tauri.conf.json` → `version`
  - `lclaw-ui/src-tauri/Cargo.toml` → `[package].version`
- **Git 标签**：对外发布时在对应提交打 **`v{version}`**（例：`v0.3.0`），与上述字符串完全一致。
- **与 CHANGELOG 的衔接**：发版时将 `## [未发布]` 的累计内容改为 `## [x.y.z] - YYYY-MM-DD`，再在文首保留空的 `## [未发布]`；日常提交仅在 `[未发布]` 下追加条目（见「Git 与保密」）。

## 整合期开发纪律

- **最小改动**：只改当前任务需要的文件与行；禁止顺手大重构、删无关注释、改无关格式。
- **先读后写**：新代码需匹配周边命名、类型、导入风格与抽象层级；优先扩展现有函数/组件。
- **合并前自检**：在客户端目录执行 `pnpm run lint`、`pnpm run typecheck`；涉及 Tauri 时确认 `src-tauri` 能编译。
- **契约优先**：与 Gateway / HTTP 的字段、错误码、鉴权方式以 `docs/` 与双方代码为准；改契约要同步文档与调用方。

## Git 与保密

- 提交信息用**完整、可读的句子**（中文或英文统一即可），说明「改了什么、为什么」。
- **Change log**：每次 `git 提交`须同步更新仓库根目录 **`CHANGELOG.md`**（与代码改动在同一提交中）。每条记录用一两句话说明**对用户或集成方可见的影响**（新增、修复、变更、废弃等）；内部-only 重构也需简短注明范围。格式沿用文件内现有章节（建议 [Keep a Changelog](https://keepachangelog.com/)：`[未发布]` / 版本号 + 日期）。**若本提交 bump 了版本号**，须同时完成「版本号与发版」中的三处同步与（若发版）Git 标签约定。**例外**：仅调整空白、错字且无行为影响的提交，可只在提交说明中写明，不改 `CHANGELOG.md`。
- 大功能拆成**小步提交**或独立 PR，便于评审与回滚；若同一任务多次提交，可在最后一次提交里合并整理 `CHANGELOG` 条目，避免重复条目。
- 禁止提交：密钥、token、本机绝对路径、仅个人适用的配置。

## 文档

- 架构决策、接口约定、排期类说明放在 **`docs/`**。
- **未明确要求时**，不要新增或扩展说明用 Markdown（避免文档噪音）。

## 与 AI 协作（可选团队约定）

- 需要面向团队输出的说明、规则解释、PR 描述等：**简体中文**。
- AI 生成代码仍须遵守本文件、`didclaw-client.mdc` 与 `didclaw-security.mdc` 的约束。

## 与 AI 协作（可选团队约定）

- 所有注释采用英文，并且简洁就好
- 注释中不能有包含了比如采用了有AGPL,GPL,LGPL等版权争议的说明
